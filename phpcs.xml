<?xml version="1.0"?>
<ruleset name="Sloop">
    <description>Sloop Framework Coding Standard</description>

    <file>src</file>
    <file>tests</file>

    <arg name="colors"/>
    <arg value="sp"/>

    <!-- ==================== -->
    <!-- PHPDoc               -->
    <!-- ==================== -->

    <!-- PHPDoc required on methods (test code excluded) -->
    <rule ref="Squiz.Commenting.FunctionComment">
        <exclude-pattern>tests/*</exclude-pattern>
        <exclude name="Squiz.Commenting.FunctionComment.ParamNotFound"/>
        <exclude name="Squiz.Commenting.FunctionComment.IncorrectTypeHint"/>
        <exclude name="Squiz.Commenting.FunctionComment.InvalidTypeHint"/>
        <exclude name="Squiz.Commenting.FunctionComment.ScalarTypeHintMissing"/>
        <exclude name="Squiz.Commenting.FunctionComment.TypeHintMissing"/>
        <exclude name="Squiz.Commenting.FunctionComment.InvalidReturn"/>
        <exclude name="Squiz.Commenting.FunctionComment.IncorrectParamVarName"/>
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop"/>
        <exclude name="Squiz.Commenting.FunctionComment.ParamCommentNotCapital"/>
        <exclude name="Squiz.Commenting.FunctionComment.ThrowsNoFullStop"/>
        <exclude name="Squiz.Commenting.FunctionComment.EmptyThrows"/>
        <exclude name="Squiz.Commenting.FunctionComment.SpacingAfterParamType"/>
        <exclude name="Squiz.Commenting.FunctionComment.SpacingAfterParamName"/>
    </rule>

    <!-- PHPDoc required on classes (test code excluded) -->
    <rule ref="Squiz.Commenting.ClassComment">
        <exclude-pattern>tests/*</exclude-pattern>
        <!-- This project allows @template / @implements / @extends in class comments -->
        <exclude name="Squiz.Commenting.ClassComment.TagNotAllowed"/>
    </rule>

    <!-- PHPDoc required on properties (test code excluded) -->
    <rule ref="Squiz.Commenting.VariableComment">
        <exclude-pattern>tests/*</exclude-pattern>
        <exclude name="Squiz.Commenting.VariableComment.IncorrectVarType"/>
        <exclude name="Squiz.Commenting.VariableComment.TagNotAllowed"/>
    </rule>

    <!-- PHPDoc asterisk alignment -->
    <rule ref="Squiz.Commenting.DocCommentAlignment"/>

    <!-- ==================== -->
    <!-- Code quality         -->
    <!-- ==================== -->

    <!-- Cyclomatic complexity check -->
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="10"/>
            <property name="absoluteComplexity" value="20"/>
        </properties>
    </rule>

    <!-- Nesting level check -->
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="nestingLevel" value="3"/>
            <property name="absoluteNestingLevel" value="5"/>
        </properties>
    </rule>

    <!-- Unreachable code detection -->
    <rule ref="Squiz.PHP.NonExecutableCode"/>

    <!-- Detect $this usage inside static methods -->
    <rule ref="Squiz.Scope.StaticThisUsage"/>

    <!-- ==================== -->
    <!-- PHP safety           -->
    <!-- ==================== -->

    <!-- Deprecated PHP function detection -->
    <rule ref="Generic.PHP.DeprecatedFunctions"/>

    <!-- Forbid the @ error control operator -->
    <rule ref="Generic.PHP.NoSilencedErrors"/>

    <!-- PHP syntax check -->
    <rule ref="Generic.PHP.Syntax"/>

    <!-- Detect leftover Git merge conflict markers -->
    <rule ref="Generic.VersionControl.GitMergeConflict"/>

    <!-- Forbid debug functions and type-cast functions -->
    <rule ref="Generic.PHP.ForbiddenFunctions">
        <properties>
            <property name="forbiddenFunctions" type="array">
                <element key="print_r" value="null"/>
                <element key="var_dump" value="null"/>
                <element key="dump" value="null"/>
                <element key="dd" value="null"/>
                <element key="debug_print_backtrace" value="null"/>
                <element key="debug_backtrace" value="null"/>
                <element key="strval" value="null"/>
                <element key="intval" value="null"/>
                <element key="boolval" value="null"/>
                <element key="floatval" value="null"/>
                <element key="settype" value="null"/>
            </property>
        </properties>
    </rule>

    <!-- ==================== -->
    <!-- Formatting           -->
    <!-- ==================== -->

    <!-- Vertical alignment of assignment operators -->
    <rule ref="Generic.Formatting.MultipleStatementAlignment">
        <properties>
            <property name="maxPadding" value="20"/>
        </properties>
    </rule>

    <!-- ==================== -->
    <!-- Slevomat extensions  -->
    <!-- ==================== -->

    <!-- Forbid variable parsing in double-quoted strings -->
    <rule ref="SlevomatCodingStandard.Strings.DisallowVariableParsing"/>

    <!-- Forbid pass-by-reference (method parameters; internal reference assignment is sometimes needed for array operations) -->
    <rule ref="SlevomatCodingStandard.PHP.DisallowReference">
        <exclude name="SlevomatCodingStandard.PHP.DisallowReference.DisallowedAssigningByReference"/>
    </rule>

    <!--
        Unused use detection.
        searchAnnotations is mandatory. The default (false) skips PHPDoc, so a
        use referenced only from @throws / @param is misdetected as unused and
        phpcbf auto-removes it, breaking PHPDoc type references (this project
        makes @throws mandatory).
    -->
    <rule ref="SlevomatCodingStandard.Namespaces.UnusedUses">
        <properties>
            <property name="searchAnnotations" value="true"/>
        </properties>
    </rule>

    <!-- ==================== -->
    <!-- PHP version compatibility -->
    <!-- ==================== -->

    <!--
        The target is "8.5 and later". This is a safety net so that features
        PHP removes or deprecates in future versions get detected as soon as
        PHPCompatibility supports them. Currently 0 detections (testVersion
        8.0- would yield 169).
    -->
    <config name="testVersion" value="8.5-"/>
    <rule ref="PHPCompatibility"/>
</ruleset>
